import 'package:flutter/material.dart';
import 'package:flutter/cupertino.dart';
class Details_Recipe extends StatelessWidget {
  const Details_Recipe({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}